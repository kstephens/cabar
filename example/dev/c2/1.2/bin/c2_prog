#!/usr/bin/env ruby

require 'c2'

puts "c2_prog version #{C2.version}: #{ARGV.join(' ')}"

cc = Cabar::Context.current

puts "required_components = #{cc.required_components.join(', ')}"
puts "selected_components = #{cc.selected_components.join(', ')}"

exit 5

